const GLOBAL = require('./constaint');

const endUrls = {
    diet: `${GLOBAL.BASE_URL}/diet/api/offer`,
    login: `${GLOBAL.BASE_URL}diet/api/mobile`,
    homePlan: `${GLOBAL.BASE_URL}diet/api/plans`,
};

export default endUrls;